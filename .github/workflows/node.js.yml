name: Scan topic

on:
  workflow_dispatch:
  schedule: 
    - cron: "* * * * *"
  
env:
  DETA_PROJECT_KEY: ${{ secrets.DETA_KEY }}

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js 16.x
      uses: actions/setup-node@v2
      with:
        node-version: 16.x
        cache: 'npm'
    - run: |
        yarn
        yarn do
